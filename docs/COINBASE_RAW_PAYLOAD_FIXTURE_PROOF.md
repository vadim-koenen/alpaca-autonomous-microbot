# P2-011C — Coinbase Raw Payload Fixture Proof

**ADVISORY / READ-ONLY / STATIC FIXTURES ONLY**
Generated by scripts/coinbase_raw_payload_fixture_proof.py

This report uses sanitized representative fixtures to analyze what direct broker facts Coinbase makes available.

## Executive Summary

**CONCLUSION: Live logger hook remains BLOCKED.**
- Direct sell proceeds not proven from current broker response handling for exit orders.

## Key Determinations (15 Points)
1. Order/status payload: **Available and used today** via `get_order`.
2. Historical fills payload: **Not called anywhere in current codebase**.
3. filled_size is direct broker fact: **Yes** (order object).
4. average_filled_price is direct broker fact: **Yes** (order object).
5. Per-fill price/size available: **Yes (in fills list fixture)**.
6. Actual fees are direct: **Yes** (total_fees on order; per-fill fee in fills list).
7. Direct sell proceeds: **No** — not proven. Current code never polls exit order status for filled_value/total_fees on sell legs.
8. Maker/taker (liquidity_indicator) per fill: **Yes (in fills list)**.
9. Partial fills: **Order summary only** (completion_percentage + cumulative fields). No multi-record fill list in order payload.
10. Stable fill-level idempotency key proven: **Yes (trade_id/entry_id in fills list)**.
11. Recommended idempotency: order_id + (trade_id or entry_id) from fills list. order_id alone is insufficient for multi-fill orders.
12. Duplicate prevention from polling: Possible using order_id + last_update_time or processed set.
13. Current CSV logger hook safe today: **No** — blocked by missing sell proceeds proof and lack of proven stable fill-level keys for exits.
14. Recommended architecture:
   - order/status REST for entry and exit order final state (good for cumulative)
   - historical_fills REST for detailed per-fill breakdown + fees + liquidity (needed for high confidence P/L)
   - Do NOT rely solely on order summary for exits if per-fill fee/liquidity matters
15. Other notes: See detailed fixture analysis below.

## Fixture Analysis
### sample_order_filled_buy (order_status)
- filled_size: direct_broker_fact — Extracted directly from Coinbase order object in get_order response
- average_filled_price: direct_broker_fact — Extracted directly from Coinbase order object in get_order response
- filled_value: direct_broker_fact — Extracted directly from Coinbase order object in get_order response
- total_fees: direct_broker_fact — Extracted directly from Coinbase order object in get_order response
- status: direct_broker_fact — Extracted directly from Coinbase order object in get_order response
- settled: direct_broker_fact — Extracted directly from Coinbase order object in get_order response
- completion_percentage: direct_broker_fact — Extracted directly from Coinbase order object in get_order response
- last_fill_time: direct_broker_fact — Extracted directly from Coinbase order object in get_order response
- created_time: direct_broker_fact — Extracted directly from Coinbase order object in get_order response
- gross_quote_value: locally_derived — size * average_filled_price (reconstructible)
- net_quote_value: locally_derived — gross - total_fees (reconstructible from this payload)
  > Order-level payload provides good cumulative facts for the final fill state.
  > No per-fill breakdown or liquidity role per individual fill in this payload.

### sample_fills_list (historical_fills)
- price: direct_broker_fact — Per-fill fact from /historical/fills (or equivalent)
- size: direct_broker_fact — Per-fill fact from /historical/fills (or equivalent)
- fee: direct_broker_fact — Per-fill fact from /historical/fills (or equivalent)
- fee_currency: direct_broker_fact — Per-fill fact from /historical/fills (or equivalent)
- liquidity_indicator: direct_broker_fact — Per-fill fact from /historical/fills (or equivalent)
- trade_id: direct_broker_fact — Per-fill fact from /historical/fills (or equivalent)
- entry_id: direct_broker_fact — Per-fill fact from /historical/fills (or equivalent)
- order_id: unavailable — Unknown or not present in fill payload
  > Fills list provides per-fill price, size, fee, and liquidity role.
  > Strong candidate for stable idempotency when combining order_id + trade_id/entry_id.

## Final Safety Verdict

**The live append of coinbase_fill_logger remains BLOCKED.**
Reason: Actual per-leg fees/proceeds and/or stable fill-level idempotency keys are not proven from current response paths for both entry and exit orders.

---
P2-011C proof complete. No execution changes. No live hooks.